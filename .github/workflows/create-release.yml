name: Build Release

on:
  push:
    tags:
      - '*'
#      - '/^\d+\.\d+\.\d+(-\w+)?$/'

jobs:
  build:
    name: Upload Release Asset
    runs-on: ubuntu-latest
    permissions:
      contents: write

    strategy:
      matrix:
        node-version: [ 18.x ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: Build Project
        run: |
          npm install
          npm run build:prod

      - name: Clean package
        run: |
          rm -rf .git .github node_modules .gitignore

      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"

      - name: Get current datestamp
        id: datestamp
        run: echo "::set-output name=datestamp::$(date +'%T')"

      - name: Versioning
        uses: "DamianReeves/write-file-action@master"
        with:
          path: skenografia.info.yml
          write-mode: append
          contents: |
            
            # Information added by Github Action packaging script on ${{ steps.date.outputs.date }}
            version: '${{  github.ref_name }}'
            project: 'skenografia'
            datestamp: ${{ steps.datestamp.outputs.datestamp }}

      - name: Compress Dist Folder
        run: zip -r skenografia.zip ./

      - name: Create Release
        id: create_release
        uses: ncipollo/release-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          artifactErrorsFailBuild: true
          artifacts: skenografia.zip
